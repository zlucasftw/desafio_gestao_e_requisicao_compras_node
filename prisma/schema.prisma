generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

enum Role {
  USER
  APPROVER
}

enum Status {
  DRAFT
  SUBMITTED
  APPROVED
  REJECTED
}

model Users {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  password  String
  role      Role     @default(USER)
  createdAt DateTime @default(now())

  purchaseRequests  PurchaseRequests[]
  approvalHistories ApprovalHistory[]
}

model PurchaseRequests {
  id          Int      @id @default(autoincrement())
  title       String    
  description String?   @db.VarChar(255)
  quantity    Int
  status      Status   @default(DRAFT)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  products    RequestItems[]
  histories   ApprovalHistory[]

  userId      Int
  user        Users    @relation(fields: [userId], references: [id])
}

model RequestItems {
  id                Int      @id @default(autoincrement())
  title             String
  description       String?   @db.VarChar(255)
  quantity          Int
  productId         Int
  createdAt         DateTime @default(now())

  purchaseRequestId Int
  purchaseRequest   PurchaseRequests @relation(fields: [purchaseRequestId], references: [id])
}

model ApprovalHistory {
  id                Int      @id @default(autoincrement())
  action            String
  reason            String   @db.VarChar(255)
  oldStatus         Status
  newStatus         Status
  createdAt         DateTime @default(now())

  approverId        Int
  approver          Users @relation(fields: [approverId], references: [id])

  purchaseRequestId Int
  purchaseRequest   PurchaseRequests @relation(fields: [purchaseRequestId], references: [id])



    /* itemRequestId  Int
    itemRequests   RequestItems[] @relation(fields: [itemRequestId], references: [id]) */

  /* @@index([approverId, purchaseRequestId], name: "fk_approver_purchase_request") */
}

